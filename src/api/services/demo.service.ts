import { DemoModel as Demo } from '../models/demo.model';

export const getDemoItems = async () => {
  try {
    // console.log(`\nGET request successful! \n\nRunning at http://localhost:3000/demo/\n`);
  } catch (err) {
    throw new Error(err);
  }

}